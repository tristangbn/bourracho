{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "uv sync --no-dev",
      "startCommand": "uv sync --no-dev && uv run uvicorn src.asgi:application --host 0.0.0.0 --port $PORT"
    }
  }
  